# Imported Rule

This rule file is imported via @-syntax from CLAUDE.md.
It adds context that counts toward the token budget.
Always follow these conventions when writing code.
Never skip the review step before committing.
