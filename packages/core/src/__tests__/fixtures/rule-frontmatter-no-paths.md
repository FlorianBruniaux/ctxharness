---
description: General output quality rules — loads at every session
---

# Output Quality

Always prefer tables over long prose for structured data.
Use code blocks with language hints for every snippet.
Never truncate output mid-sentence.
