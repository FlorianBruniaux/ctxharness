[project]
name = "my-python-app"
version = "1.2.3"
requires-python = ">=3.11"
dependencies = [
    "django>=4.2.0",
    "requests>=2.28.0",
]

[tool.poetry]
name = "my-python-app"
version = "1.2.3"

[tool.poetry.dependencies]
python = "^3.11"
django = "^4.2.0"
sqlalchemy = { version = "^2.0.0", extras = ["asyncio"] }
